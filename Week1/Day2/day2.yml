Day 02
07:00 to 07:50 -> var, let, const
07:50 to 08:05 -> BreakOut
08:05 to 08:15 -> Break
08:15 to 09:00 -> operators,Conditional statements





var:

What
var is the oldest way to declare variables in JavaScript (before ES6).

Scope behavior
Function-scoped
Not block-scoped (ignores {} blocks like if, for)

Redeclaration & reassignment
Redeclaration allowed
Reassignment allowed

var empName='Dilip'
empName=345

console.log(empName)//output -> 345

var comName="TestLeaf"
var comName="Qeagle"

Problems
Can cause unexpected bugs
Values may change unintentionally
Not recommended in modern JavaScript

Example

function test() {
  if (true) {
    var x = 10;
  }
  console.log(x);
}
test();

// Output: 10


Reason
var ignores block scope and leaks outside the if block.

let:

What
let is a modern variable declaration introduced in ES6.

Scope behavior
Block-scoped
Limited to {} block (if, for, while, etc.)

Redeclaration & reassignment
Redeclaration not allowed in same scope
Reassignment allowed

let count =50
count=51

Why let is better than var
Prevents accidental overwrites
More predictable behavior
Recommended for variables that change

Example

let y = 10;
if (true) {
  let y = 20;
  console.log(y);
}
console.log(y);

// Output:
// 20
// 10


Reason
Each block has its own y variable.

const:

pi- 3.14

What
const is used to declare constant values.

Scope behavior
Block-scoped (same as let)

Redeclaration & reassignment
Redeclaration not allowed
Reassignment not allowed
Must be initialized at declaration

Important note (Objects & Arrays)
const prevents reassignment, not modification

Example

const user = { name: "Ravi" };
user.name = "Kumar";   // Allowed

// user = {}           // Not allowed


Why const is preferred
Ensures safety
Improves readability
Prevents accidental reassignment

operators:


 arithmentic
+
-
/  -->quotient
*
%  --> 1 (remainder)

comparision of data  -> return type boolean
<
6<5 -> false
>
7>5 ->true
>=
3>=4 ->false
<=
==
!=

7>3 -> true
5!=5-> false

pre/post -> automatic only post incerment and decerment

pre incerment (++i)
post incerment (i++):
i++=> i+1
let i=0

++i=1+i
console.log(i++) //0
console.log(i) //1
post  decerment(i--)
i-- => i-1


let num=5
for(let i=0;i<num;i++) 
//old value + 1
0+1=1
1+1
++  --> data +1

post decerment (--)
-- -->data-1

logical - boolean

&&-> AND , || ->OR 

(5==5 && 6<5)
(true && false) = false

(5==5 || 6<5)
(true || false)= true




ClassRoom 2:

var num-> 45
//re-assignment
//re-declaration

let empname -> "testing"
//re-assignment
//re-declaration

const final=34
//re-assignment
//re-declaration




Control flow statements
========================
Conditional statement:

if - condition
===============
let i=-6
-> 1 if condition
if(i>0){ 
//when condition is true then only if block will be execution
}



-> 2 if-else condition
let i=-6
if(i>0){
// if condition will false
}else{
//it will go to else block
}




-> 3 if ->else if ->else 
let age =20

 if(age<18){ //false
 console.log("no voter id")
}else if(age>=18){ //true
console.log("voter id")
}else{
console.log("default id")
}


if(){
}else if(){
}else if(){
}else if(){
}else if(){
}else{
}


ClassRoom 2:
let mark =75
mark>=90 -> A grade
mark>=80 -> B grade
mark>=70 -> C grade
else -> fail